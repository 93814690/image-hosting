<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="top.liyf.imagehosting.dao.ImageDao">
    <insert id="saveImage">
        insert into t_image(image_id, image_name, uid, create_time, url)
        values(#{imageId},#{imageName},#{uid},#{createTime},#{url})
    </insert>
</mapper>